import 'package:intl/intl.dart';

final formatter = NumberFormat.simpleCurrency(
  decimalDigits: 0,
  name: 'UGX ',
);


final quantityFormatter = NumberFormat.simpleCurrency(
  decimalDigits: 0,
  name: 'Quantity: ',
);
